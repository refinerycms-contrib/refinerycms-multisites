<%= render 'refinery/admin/search_header', :url => refinery.multisites_admin_multisites_accounts_path %>
<div class='pagination_container'>
  <% if @accounts.any? %>
    <%= render 'accounts' %>
  <% else %>
    <p>
      <% if searching? %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% else %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% end %>
    </p>
  <% end %>
</div>
