Refinery::Multisites.configure do |config|
  # Single tenant mode
  config.single_tenant_mode = <%= Refinery::Multisites.single_tenant_mode.inspect %>

  # Restricted subdomains who can not be used for multitenancy
  config.restricted_subdomains = <%= Refinery::Multisites.restricted_subdomains.inspect %>

  # If you're grafting onto an existing app, change this to your User class
  # (eg: Refinery::Multisites.user_class = "Refinery::Authentication::Devise::User" )
  <%= "# " if (user_class = Refinery::Multisites.user_class.to_s).blank? %>Refinery::Multisites.user_class = <%= user_class.inspect %>
end